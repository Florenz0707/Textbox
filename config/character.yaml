# Characters configuration (new schema)
# Runtime order is lexicographic by english id (the keys below).
# `font` is optional; default is font3.ttf.

character:
  momoi:
    name: "才羽桃井"
    color: [255, 185, 193]
  sherri:
    name: "橘雪莉"
    color: [69, 138, 220]
